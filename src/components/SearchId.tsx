const SearchId: React.FC = () => {
  return (
    <form>
      <input placeholder="Поиск идентификатора (Простой список)"></input>
      <button type="button">Найти</button>
    </form>
  );
};

export default SearchId;
